# BonsaiNFTArt サイト運用手順書

新しいセッションを始めるときは、まずこのファイルを開いて Claude に見せること。
これを見せれば、辻が記憶をたどる必要はない。

## 0. 基本情報

- **作業するフォルダ**：`C:\Users\tsubu\Desktop\bonsai-nft-art`
  - ここが Git リポジトリ本体。GitHub 上のリポジトリ名も `bonsai-nft-art`。
- **公開URL**：https://amg02201832-cpu.github.io/bonsai-nft-art/
- **旧フォルダ**：`C:\Users\tsubu\Desktop\BONSAI_NFTプロジェクト`
  - Cursor 時代の旧環境。中の index.html / script.js / style.css / .git は**残骸なので触らない**。
  - images は OpenSea 用の原本置き場として残している。
- 開発ツールは VS Code + Claude Code。

## 1. Claude Code に必ず守らせるルール

プロンプトに毎回書くこと。

- `node -e` は使わない。必要なら temp `.js` ファイル経由で実行する。
- `&&` や `;` によるコマンド連結は使わない。
- `git add` は**ファイル名を明示指定**する。`-A` や `.` は使わない。
- 承認は毎回「1 Yes」の単発のみ。パターン許可・セッション許可は選ばない。
- **「変更後の想定」ではなく「実際のファイルの内容」を報告させる**。
  - 実績あり：想定を提示されて未編集のまま進みかけたことがある。
  - 確認したいときは `git diff ファイル名` の出力を見せてもらうのが確実。

## 2. 新コレクション追加の手順

### 手順1：画像を用意する

- `images\（コレクション名）\` フォルダを作る。
  - フォルダ名は全て大文字。`BONSAI XXXX` 形式と接頭辞なし形式が混在しているが、どちらでもよい。
- 4作の画像を入れ、**数字のみ**にリネームする。
  - `1.png` `2.png` `3.png` `4.png`（起承転結の順）
  - 日本語ファイル名は使わない（URLエンコードで事故る）。
- **サムネイル画像も必要**。`サムネ.png` として同じフォルダに入れる。
  - Midjourney で `--ar 1:1` で生成する。既存は 1024×1024。

### 手順2：WebP に変換する

Claude Code に依頼：

```
images/（コレクション名）/ の中の PNG を WebP に変換してください。

既存の temp-webp.js の dir を '（コレクション名）' に書き換えて実行してください。
条件は既存と同じ（長辺1024pxにリサイズ、quality 82）。

node -e は使わず、temp .js ファイル経由で実行してください。
実行前に確認を取ってください。
```

PNG は消さずに残す（既存コレクションも両方持っている）。

### 手順3：index.html にカードを追加する

Claude Code に依頼：

```
index.html に （コレクション名）のカードを追加してください。

追加位置：直前のコレクションのカードの直後

既存カードと全く同じ構造で、以下の値に置き換えてください。
- コメント番号：（番号）
- data-collection: （小文字のキー名）
- サムネ画像パス: images/（フォルダ名）/サムネ.webp と サムネ.png
- img の alt: （コレクション名）thumbnail
- card-num: （番号）
- card-title: （日本語 / ローマ字）
- card-desc の data-ja / data-en
- card-link の href: OpenSea の URL

編集後、追加したHTMLをそのまま表示してください。
実行前に確認を取ってください。
```

`data-collection` は**小文字**。script.js のキー名と一致させる。

### 手順4：script.js にデータを入れる（5回に分ける）

長い日本語が途中で切れるのを防ぐため、必ず分割する。

**①空の器**

```
script.js に （キー名）コレクションの空の器を追加してください。

追加位置：直前のコレクションの直後

（キー名）: {
  meta: { ja: '全4作品・0.06〜0.12 WETH', en: '4 works · 0.06–0.12 WETH' },
  works: [
  ]
},

works の中身は空のままにしてください。

node -e は使わず、必要なら temp .js ファイル経由で実行してください。
編集後、差分を見せてください。実行前に確認を取ってください。
```

**②〜⑤各1作ずつ**

```
script.js の （キー名）コレクションの works 配列に、（N）作目を追加してください。
（N-1）作目の直後に入れてください。

{
  base: 'images/（フォルダ名）/（N）',
  titleJa: '（起/承/転/結）・（題）「（副題）」',
  titleEn: '（Ki/Shō/Ten/Ketsu） – （英題）',
  descJa: '（日本語説明文）',
  descEn: '（英語説明文）'
},

node -e は使わず、必要なら temp .js ファイル経由で実行してください。
編集後、差分を見せてください。実行前に確認を取ってください。
```

4作目だけ末尾のカンマなし。

### 手順5：統計数値を更新する（忘れやすい）

**カードを追加しても、トップの数字は自動更新されない。**

```
index.html の統計数値を更新してください。

- コレクション数：（旧）→（新）
- 作品数：（旧）→（新）

hero-meta-num と section-sub（日本語・英語両方）の全箇所を更新してください。

更新後、git diff index.html の出力を見せて、
実際にファイルが変更されたことを確認させてください。

&& や ; によるコマンド連結、node -e は使わないでください。
実行前に確認を取ってください。
```

### 手順6：ローカルで確認する

`npx serve` は **VS Code のターミナルではなく、独立した PowerShell ウィンドウ**で起動する。

1. Windows の検索から PowerShell を起動
2. `cd C:\Users\tsubu\Desktop\bonsai-nft-art`
3. `npx serve`
4. ブラウザで `http://localhost:3000`
5. カード表示・モーダル4作品・統計数値を確認

このウィンドウは閉じない（閉じるとサーバーが止まる）。

### 手順7：コミットして公開する

```
（コレクション名）の追加をコミットして push してください。

git add は以下を明示指定してください。-A や . は使わないでください。
- index.html
- script.js
- images/（フォルダ名）/

コミットメッセージ：
Add （コレクション名） collection （番号）th

その後 git push origin main を実行してください。

&& や ; によるコマンド連結は使わないでください。
実行前に確認を取ってください。
```

### 手順8：公開サイトで確認する

push から1〜3分待つ（GitHub Pages のビルド時間）。

1. https://amg02201832-cpu.github.io/bonsai-nft-art/ を開く
2. **`Ctrl + F5` で強制リロード**（通常リロードだとキャッシュで古いまま）
3. 統計数値、カード、モーダル、日英切り替えを確認

画像が出ず枠だけになっている場合は、フォルダ名の大文字小文字を疑う。
GitHub Pages は Linux 上なので大文字小文字を厳密に区別する。

## 3. 落とし穴チェックリスト

- [ ] 統計数値（コレクション数・作品数）を更新したか
- [ ] サムネ画像を作ったか
- [ ] ファイル名は数字のみか（日本語名になっていないか）
- [ ] `data-collection` は小文字か
- [ ] script.js のキー名と `data-collection` が一致しているか
- [ ] 4作目の末尾にカンマが付いていないか
- [ ] OpenSea の URL は仮のままになっていないか
- [ ] 公開後に `Ctrl + F5` で確認したか

## 4. データ構造の参考

script.js のコレクションデータ：

```javascript
（キー名）: {
  meta: { ja: '全4作品・0.06〜0.12 WETH', en: '4 works · 0.06–0.12 WETH' },
  works: [
    {
      base: 'images/（フォルダ名）/1',
      titleJa: '起・（題）「（副題）」',
      titleEn: 'Ki – （英題）',
      descJa: '（説明文）',
      descEn: '（説明文）'
    },
  ]
},
```

`base` は拡張子なし。`.webp` と `.png` が自動で使い分けられる。
